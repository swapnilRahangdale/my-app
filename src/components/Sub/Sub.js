const Sub = (abc) => {

    return(

        <div>
            <p>This is Sub Component</p>
            <p>Substraction = {parseInt(abc.s)-parseInt(abc.t)}</p>
        </div>

    )

}

export default Sub;