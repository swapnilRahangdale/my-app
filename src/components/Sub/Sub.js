const Sub = (abc) => {

    return(

        <div>
            <p>This is a Sub Component 1</p>
            <p>Substraction = {parseInt(abc.s)-parseInt(abc.t)}</p>
        </div>

    )

}

export default Sub;